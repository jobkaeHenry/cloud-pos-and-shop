export const GET_SHOP_SETTING = (domain: string) => `/setting/${domain}`;

export const GET_MENU_LIST = (domain: string) => `/menu/${domain}`;

export const GET_COUPON_LIST = (domain: string) => `/coupon/${domain}`;
